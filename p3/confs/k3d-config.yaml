apiVersion: k3d.io/v1alpha5
kind: Simple

metadata:
  name: iot

servers: 1
agents: 0

ports:
  - port: 8888:8888
    nodeFilters:
      - loadbalancer

options:
  k3d:
    wait: true
    timeout: "120s"
